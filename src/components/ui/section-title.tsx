const SectionTitle = ({ title }: { title: string }) => (
    <h2 className="text-primary text-xl font-semibold w-full border-b-2 border-secondary leading-10">{title}</h2>
);

export default SectionTitle;